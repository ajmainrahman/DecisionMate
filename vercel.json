{
  "version": 2,
  "buildCommand": "BASE_PATH=/ PORT=3000 pnpm --filter @workspace/decisionmate run build && pnpm --filter @workspace/api-server run build",
  "outputDirectory": "artifacts/decisionmate/dist/public",
  "installCommand": "npm install -g pnpm@latest && pnpm install --frozen-lockfile --ignore-scripts",
  "framework": null,
  "env": {
    "BASE_PATH": "/",
    "PORT": "3000"
  },
  "functions": {
    "api/[[...route]].mjs": {
      "includeFiles": "artifacts/api-server/dist/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[[...route]]" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
